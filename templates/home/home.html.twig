{% extends 'base.html.twig' %}

{% block body %}

    <div class=" ">
    <div class="jumbotron  bg1">
        <div class="b3">
            <div class="row justify-content-center">
                <div class="">
                    {{ form_start(form) }}
                    <div class="form-row input_">
                        <div class="col-md-6">
                            {{ form_row(form.titre) }}
                        </div>
                        <div class="col-md-4">
                            {{ form_row(form.maxprix) }}
                        </div>
                        <div class="col">
                            <button type="submit"  class="btn btn-search "><img src="{{ asset('img/icons8_search_20px.png') }}" alt=""><span></span></button>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col">
                {{ form_end(form) }}
                        <div class="logo">
                            <img src="/img/icons8_facebook_25px_1.png" title="GeniusGeek">
                            <img src="/img/icons8_instagram_25px_1.png" title="@GeniusGeek">
                            <img src="/img/icons8_whatsapp_25px_1.png" title="+228 93240584">

                        </div>

            </div>

        </div>



    </div>
                <div class="row ">
                 {{ include('sidebar.html.twig') }}
                    <div class=" p1 ">
                        <div class="row">
                            <div class=" mx-4 span3-div">
                                <span class="  "> <img src="{{ asset('img/icons8_dvd_logo_25px.png') }}" alt=""> <span class="span1">Dernier Ajouter</span> </span>
                            </div>
                        </div>
                        <div class="my-5  disque " >
                            {% for proprity in proprieter %}
                                <div class=" proprieter_shadow">
                                    <div class="div_img ">
                                        {% if proprity.filename %}
                                            <img class="" src="images/disques/{{  proprity.filename }}" alt="{{  proprity.filename }}">
                                        {% else %}
                                            <img class="" src="{{ asset('images/disques/empty.jpg') }}" alt="{{  proprity.filename }}">
                                        {% endif %}
                                    </div>
                                    <div class=" text1">
                                        <a class="text-dark" href="{% if app.user %} {{ path('propertyshow',{id: proprity.id,user: app.user.id }) }} {% else %}  {{ path('propertyshow',{id: proprity.id,user: 1 }) }} {% endif %}" class="">{{ proprity.titre }}</a><br>

                                    </div>
                                </div>

                            {% endfor %}
                        </div>

                    </div>

             </div>


{% endblock %}